apiVersion: mesh-manager.nadundesilva.github.io/v1alpha1
kind: Application
metadata:
  name: application-sample
spec:
  podSpec:
    containers:
    - name: nginx
      image: nginx:1.14.2
      ports:
      - containerPort: 80
